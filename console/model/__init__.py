"""The entity model — seven closed kinds (console-policy.md §2.1).

The kinds are exhaustive and add-by-PR-only against the policy, not by adding
a case here. Cost, owner, authority tier, lifecycle, substrate, repo and
environment are FACETS on entities (§2.2), never kinds.
"""
